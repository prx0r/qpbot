# core — the running game (stdlib only)

Arena truth, hash-chained ledger, red-team loop, tournament, autopilot,
guards, CLI, status feed. Every claim settles server-side; receipts verify.

Play: `python3 -m core.cli tournament`, `... autopilot --rounds 3`.
Freeze rule: verifier behavior changes need a new test contract.
