# agentcom — headless control plane (skeleton, growing)

`htasks/` human queue with leases. `lanes/` Seed0 frozen-root runner.
`ledger/` spend totals. `services/` daemon with writer exclusivity.
`runtime/` Pi lane specs. `interfaces/` ACP routing + AgentDeck tiles.
Canonical ports land here with `Provenance:` headers; see DEV_PLAN Phase 2.
